<h2>ブログ投稿</h2>

<%= form_with model: @blog, url: admin_blogs_path(@blog), local:true do |f| %>

<h4>画像</h4>
<%= f.attachment_field :image %>

<h4>タイトル</h4>
<%= f.text_field :title %>

<h4>行ったこと</h4>
<%= f.text_area :body %>

<%= f.submit '投稿' %>

<% end %>
